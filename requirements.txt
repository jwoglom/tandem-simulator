# Tandem Mobi Insulin Pump Simulator - Production Dependencies

# BLE/Bluetooth (Milestone 1)
# D-Bus bindings for BlueZ communication
# Note: On some systems (especially CI), you may want to use system packages instead:
#   sudo apt-get install python3-gi python3-gi-cairo python3-dbus
# and use requirements-ci.txt which excludes these packages.
dbus-python>=1.3.2
PyGObject>=3.42.0

# Cryptography (Milestone 2-3)
# For HMAC, JPake, and message signing
cryptography>=41.0.0

# Terminal UI (Milestone 5)
# Rich library for beautiful terminal output and TUI
rich>=13.7.0
textual>=0.44.0

# Configuration and Data
# YAML for configuration files
PyYAML>=6.0

# Optional: Web API (Milestone 6)
# flask>=3.0.0
# flask-cors>=4.0.0
